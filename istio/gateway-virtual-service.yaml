apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: muffin-wallet-virtual-service-for-gateway
spec:
  hosts:
    - wallet.example.com
  gateways:
    - muffin-wallet-istio-ingress-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: muffin-wallet
            port:
              number: 80
