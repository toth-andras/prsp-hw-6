apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: muffin-wallet-istio-ingress-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - wallet.example.com
