apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: destination-rules-ingress
spec:
  host: host.minikube.internal
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 30
        connectTimeout: 10s
    tls:
      mode: DISABLE